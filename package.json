{
  "name": "koco-demo-image-picker",
  "version": "1.0.0",
  "homepage": "https://github.com/cbcrc/koco-demo-image-picker",
  "authors": [
    "Frédérik Charest & Maxime Séguin"
  ],
  "license": "MIT",
  "scripts": {
    "build": "rm -rf dist && mkdir dist && cp -r src/* dist && rm -rf dist/*.js && babel src --out-dir dist --modules umd --presets es2015,stage-0"
  },
  "dependencies": {
    "koco-knockout-utilities": "^0.3.2",
    "koco-dialoger": "^0.4.0"
  }
}
